class Sale
    Attributes
        ID: Integer PK,
        Products[]: Array<Product>,
        TotalValue: Float,
        CustomerId: Customer

    Methods
        CalculateTotalValue
            if(!Products.empty)
                for(Products in Product)
                    TotalValue += Product.Price
            else 
                return 0
